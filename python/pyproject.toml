[project]
name = "gameonpaper-python"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "Flask",
    "sportsdataverse==0.0.71",
    "gunicorn",
    "psycopg[binary]>=3.1",
]

[tool.uv.sources]
sportsdataverse = { git = "https://github.com/sportsdataverse/sportsdataverse-py" }

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
