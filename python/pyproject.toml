[project]
name = "gameonpaper-python"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "Flask",
    "sportsdataverse",
    "gunicorn",
]

[tool.uv.sources]
sportsdataverse = { git = "https://github.com/sportsdataverse/sportsdataverse-py", branch = "main" }
