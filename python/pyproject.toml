[project]
name = "gameonpaper-python"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "Flask",
    "sportsdataverse>=0.0.73",
    "gunicorn",
    "flask-compress>=1.24",
    "orjson>=3.11.9",
    "psycopg[binary]>=3.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
